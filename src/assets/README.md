# Asset conventions

Use this folder for assets imported by TypeScript, TSX, or CSS so Vite can
fingerprint and copy them into the static production build.

- `audio/`: short effects, spoken-number clips, and ambient loops.
- `images/`: images, textures, and other raster visual assets.

Keep files in `public/` only when they must be served with an unchanged path.
